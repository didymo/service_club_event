{#
/**
 * @file volunteer_registration.html.twig
 * Default theme implementation to present Volunteer registration data.
 *
 * This template is used when viewing Volunteer registration pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_volunteer_registration()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('volunteer_registration') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
